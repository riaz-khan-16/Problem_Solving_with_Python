#Find Ncr & Npr

# Ncr (Combinations):

# Ncr = n! / (r! * (n-r)!)

# where n is the total number of items and r is the number of items being chosen.

# Npr (Permutations):

# Npr = n! / (n-r)!


n=int(input("the total number of items: "))
r=int(input("the number of items being chosen: "))
